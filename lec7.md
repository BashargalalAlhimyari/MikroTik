
إليك ملخص شامل لمجريات المحاضرة:

### 1. مفهوم الراديوس والفرق عن المودم المنزلي
أوضح المحاضر أن الفرق الجوهري يكمن في أن المودم المنزلي يستخدم كلمة سر مشتركة للجميع، بينما يتميز **الراديوس (RADIUS)** بأن لكل مستخدم اسم مستخدم وكلمة سر خاصة به، مما يتيح تحويل المودم أو الراوتر إلى نظام إداري متكامل [3-5].

### 2. إعدادات نظام الراديوس (Radius Server)
بدأت الخطوات العملية بضبط إعدادات الراديوس داخل الميكروتيك:
*   **العنوان (Address):** تم تحديد عنوان خاص باليوزر مانجر، مثل `192.168.200.200` [6, 7].
*   **كلمة السر (Secret):** وضع كلمة سر للأمان (مثلاً `12345`) لضمان عدم دخول أي شخص غير مخول [6, 7].
*   **المنفذ (Port):** تم تحديد بورت معين (مثل `15000`) وتفعيل خيار "Accept" في إعدادات "Incoming" [6, 7].
*   **إضافة العنوان في الراوتر:** يجب إضافة العنوان الجديد في قائمة `IP Address` داخل الميكروتيك ليتعرف عليه النظام [6, 7].

### 3. الوصول إلى واجهة User Manager وإصلاح المشكلات
يتم الدخول إلى واجهة التحكم عبر المتصفح بكتابة العنوان المتبوع بـ `/userman` (مثلاً `192.168.200.200/userman`) [8, 9].
*   **حل مشكلات تسجيل الدخول:** في حال واجهت النظام مشكلة في قاعدة البيانات أو كلمة سر قديمة، يُنصح بحذف ملف اليوزر مانجر من قائمة `Files` وإعادة التشغيل ليقوم النظام بإنشائه من جديد بدون كلمة سر [10, 11].
*   **التحقق من الحزم (Packages):** يجب التأكد من وجود حزمة "user-manager" منصبة داخل الجهاز [12, 13].

### 4. الربط بين User Manager والراديوس
تعد هذه الخطوة أساسية، وتتم من داخل واجهة اليوزر مانجر في المتصفح عبر تبويب **Routers**:
*   إضافة راوتر جديد (Add New) وتسميته.
*   إدخال نفس الـ IP (`192.168.200.200`) ونفس كلمة السر (`12345`) التي تم وضعها في إعدادات الراديوس لضمان المطابقة والارتباط [14, 15].

### 5. إنشاء الباقات (Profiles) والمحددات (Limitations)
شرحت المحاضرة آلية إنشاء العروض للزبائن:
*   **إنشاء الباقات (Profiles):** تم إنشاء نماذج مثل باقة "أبو 200" (صالحة لـ 3 أيام) وباقة "أبو 500" (صالحة لـ 7 أيام) [16-19].
*   **المحددات (Limitations):** هي القيود التي توضع على الباقة، مثل كمية البيانات (500 ميجا أو 1 جيجا) والوقت الفعلي للاستخدام (مثلاً 5 ساعات أو 10 ساعات) [20, 21].
*   **الربط:** يجب ربط كل باقة (Profile) بالمحدد (Limitation) الخاص بها لتعمل القيود بشكل صحيح [22, 23].

### 6. إنشاء المستخدمين (Users)
*   **الإضافة الجماعية (Batch):** يمكن إنشاء مجموعة كروت دفعة واحدة (مثلاً 100 كرت) [24, 25].
*   **الأمان:** يفضل أن يكون طول اسم المستخدم وكلمة السر كبيراً (8 خانات فأكثر) لتصعيب عملية التخمين، كما يمكن جعل كلمة السر هي نفسها اسم المستخدم لتسهيل الدخول [24-27].

### 7. تصميم وطباعة الكروت
*   يتم استخدام أكواد **HTML** وقوالب جاهزة (Templates) لتصميم شكل الكرت [28-31].
*   يتم رفع صور الخلفية للكروت إلى مجلد `hotspot` في الميكروتيك [28, 30].
*   يتم توليد الكروت (Generate) من قائمة المستخدمين لاختيار القالب المطلوب وطباعته [32, 33].




اثراء اضافي💥💥💥




### 1. فلسفة النظام: التميز عن "المودم" التقليدي
أوضح المحاضر أن الفارق الجوهري هو أن المودم المنزلي يستخدم مفتاح تشفير مشترك (Shared Key) حيث يدخل الجميع بنفس كلمة السر، بينما في **الراديوس/يوزر مانجر**، يمتلك كل مستخدم "بروفايل" خاصاً به يتضمن اسم مستخدم وكلمة سر فريدة [2، 33]. هذا النظام يسمح بتحويل أي جهاز (سواء كان راوتر ميكروتيك أو حتى كمبيوتر منزلي منصب عليه نظام راديوس) إلى مدير صلاحيات متطور [2، 33].

### 2. خيارات الاستضافة (Local vs External)
أشار المحاضر إلى أن "اليوزر مانجر" في الأصل هو عبارة عن كمبيوتر مستقل، ولكن شركة ميكروتيك قامت بدمجه داخل الراوتر لتوفير التكاليف [3، 34].
*   **الاستضافة الداخلية:** يمكن استخدام العنوان المحلي `127.0.0.1` إذا كان النظام داخل الراوتر نفسه [1].
*   **الاستضافة الخارجية:** إذا كان النظام على جهاز آخر، يجب تحديد عنوان IP خاص به (مثل `192.168.200.200`) ليرتبط به الميكروتيك [3، 4].

### 3. إجراءات الطوارئ وإصلاح قاعدة البيانات
في حال تعذر الدخول إلى واجهة اليوزر مانجر بسبب نسيان كلمة السر أو حدوث خطأ في قاعدة البيانات، ذكر المحاضر خطوات محددة:
*   الذهاب إلى قائمة **Files** في الميكروتيك [7، 38].
*   **حذف مجلد "user-manager"** بالكامل؛ هذا الإجراء يقوم بمسح الكروت وقاعدة البيانات القديمة، وعند إعادة تشغيل الجهاز (Reboot)، يقوم النظام بإنشاء ملف جديد تلقائياً بدون كلمة سر (صفرية) [7، 38].
*   يجب التأكد دائماً من وجود حزمة (Package) تسمى `user-manager` في قائمة الحزم ليعمل النظام أصلاً [6، 37].

### 4. الربط الشبكي العميق (IP & Ports)
*   **تعريف العنوان للراوتر:** لا يكفي تحديد IP لليوزر مانجر داخل إعدادات الراديوس فقط، بل يجب إضافته يدوياً في قائمة `IP > Addresses` مع تحديد المنفذ (Interface) لكي يستطيع الراوتر "رؤية" هذا العنوان والوصول إلى واجهة المتصفح [4، 5، 35، 36].
*   **المنافذ (Ports):** تم استخدام البورت `15000` كمثال، وهو رقم يمكن اختياره بحرية من ضمن 65,535 بورت متاح، بشرط المطابقة بين إعدادات الراديوس وإعدادات الراوتر داخل واجهة اليوزر مانجر [4، 11، 42].

### 5. منطق صلاحية الكروت (الزمن الفعلي vs الصلاحية)
شرح المحاضر بدقة كيف يتم التحكم في الوقت:
*   **حالة العروض الموقوتة (مثل عرض رمضان):** يمكن ضبط الكروت لتبدأ وتنتهي في تاريخ محدد (مثلاً من 1 رمضان إلى 30 رمضان)؛ هنا تنتهي صلاحية الكرت للجميع في نفس اللحظة بغض النظر عن وقت الشراء [13، 44].
*   **الحالة العامة:** يبدأ عداد الصلاحية (مثلاً 3 أيام) من لحظة **أول تسجيل دخول** (First Login) للمستخدم [12، 43].
*   **الوقت الفعلي (Uptime):** يختلف عن الصلاحية؛ فقد يكون الكرت صالحاً لمدة 3 أيام، لكن الوقت الفعلي المسموح للمستخدم بالبقاء فيه متصلاً هو 5 ساعات فقط خلال تلك الأيام [15، 46].

### 6. تفاصيل برمجية في تصميم الكروت (Templates)
عند تصميم الكروت، هناك خطوات برمجية دقيقة ذكرها المحاضر:
*   **تعديل كود HTML:** يجب فتح ملف الكود (Card Code) وتعديل الروابط؛ فإذا كان الكود يحتوي على رابط افتراضي مثل `ezzedeen.com` والموقع الفعلي هو `dc.com` مثلاً، فلن تظهر الصور ما لم يتم تعديل الرابط داخل الكود ليطابق اسم الصفحة المحلية [21، 52].
*   **مسار الصور:** يتم رفع الصور إلى مجلد `hotspot` حصراً لضمان ظهورها كخلفيات للكروت [20، 51].
*   **إعدادات المتصفح:** عند محاولة الطباعة وظهور الكروت بيضاء بدون خلفية، يجب تفعيل خيار "Background Graphics" من إعدادات الطباعة في المتصفح [22، 53].

### 7. أمان المستخدمين
أكد المحاضر على أن طول اسم المستخدم وكلمة السر يجب أن يكون **8 خانات فأكثر** (يفضل 10 أو 12) لجعل عمليات التخمين (Brute Force) صعبة جداً، بينما في التجارب المعملية تم استخدام 3 خانات فقط للتسهيل [17، 18، 48، 49]. كما يمكن تفعيل خيار يجعل كلمة السر هي نفسها اسم المستخدم لتسهيل العملية على الزبائن [18، 49].
